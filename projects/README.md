# Local Project Outputs

This folder is where each teammate's local AI BI project workspaces are created:

```text
projects/<Project_Name>/
```

Generated project folders, source inventories, logs, uploaded evidence, SQL outputs, validation notes, and runtime state are intentionally ignored by Git. Do not upload local project outputs, raw data, credentials, or customer-specific files unless they have been separately reviewed and approved.
